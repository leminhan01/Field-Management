import React from 'react';
import ScreenShell from '../../components/layout/ScreenShell';

const CheckInScreen = () => {
  return <ScreenShell title="Check-in" subtitle="Xác nhận vị trí GPS tại cửa hàng" />;
};

export default CheckInScreen;
